# Backend API URL — set this in Vercel project settings for production
# For local development, leave unset (Vite proxy handles it → localhost:8000)
# VITE_API_URL=https://sentinel-space-api.onrender.com/api
